# Supabase Database Schema for MobileApp

## Setup Instructions

1. **Create new Supabase project** (if not exists)
2. **Enable Row Level Security (RLS)** on all tables
3. **Run migrations in order:**
   ```bash
   # In Supabase SQL Editor, run files sequentially:
   1. schema.sql     # Tables & relationships
   2. rls.sql        # Security policies  
   3. functions.sql  # Helpers & triggers
   4. indexes.sql    # Performance indexes
   5. seeds.sql      # Sample data (optional)
   ```

4. **App Integration:**
   ```typescript
   // Install: npm i @supabase/supabase-js
   import { createClient } from '@supabase/supabase-js'
   const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)
   ```

5. **RLS Notes:** All policies use `auth.uid() = user_id`

## Tables Overview (14 tables)
- `profiles` - User profile extensions
- `daily_metrics` - Daily calories/steps/etc
- `goals` - User goals & progress  
- `habits` - Reminders & tracking
- `exercises` - Exercise catalog
- `recipes`, `foods`, `meals` - Nutrition database
- `sticky_notes` - Dashboard notes
- `weekly_reports` - Aggregated reports
- Views & functions for reports

## Key Features
- ✅ User-specific RLS
- ✅ Time-series data (daily/weekly)
- ✅ JSON fields for flexible data (ingredients, breakdowns)
- ✅ Indexes for common queries
- ✅ Triggers for auto-calculations
- ✅ Seed data matching app mocks

---

**Last Updated:** Auto-generated by BLACKBOXAI
